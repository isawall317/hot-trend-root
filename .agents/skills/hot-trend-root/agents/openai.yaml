interface:
  display_name: "Hot Trend Root"
  short_description: "Repo-specific patterns and workflows for hot-trend-root"
  default_prompt: "Use the hot-trend-root repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true